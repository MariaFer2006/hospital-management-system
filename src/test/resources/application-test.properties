# Test Configuration
# ==================

# Test Database Configuration
database.url=jdbc:h2:mem:testdb
database.username=test
database.password=test
database.driver-class-name=org.h2.Driver

# Test-specific settings
hospital.test.mode=true
hospital.test.data.enabled=true

# Logging for tests
logging.level.root=WARN
logging.level.app=DEBUG

# Test Business Rules (relaxed for testing)
hospital.working.hours.start=00:00
hospital.working.hours.end=23:59
hospital.appointment.duration.minutes=15
hospital.max.appointments.per.day=100
